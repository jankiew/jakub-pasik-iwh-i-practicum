doctype html
html
  head
    meta(charset="utf-8")
    title= title
  body
    h1= title
    if error
      p(style="color:red") Error: #{error}
    else if data && data.length
      ul
        each record in data
          li= record.properties.application_name
    else
      p No records found yet.